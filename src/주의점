1.Q파일은 절대 GIT에서 관리 X
2.queryDSL에는 컴파일 타임에 에러 검출 가능
  (JPQL은 실행 시점에 오류발생)
3. 동시성 문제
   -> 엔티티 매니저는 트렉젝션마다 별도의 영속성 컨텍스트 제공
   -> 동시성 문제 해결
   if( 더티체킹 ) => JPQL을 이용한 동시성문제 해결
3.from절에는 subQuery불가능
   => join으로 변경
   => 쿼리 분리
   => native sql 사용
   - 도서 : SQL AntiPatterns ,
4. case문에서 바꾸는건 꼭 필요한 경우에만
5. 같은테이블을 조인하는경우x -> 기본인스턴스 사용 -> 같이 조인하는경우 Q타입 생성해서 하면됨
6. 페이징 할떄 count쿼리는 조인이 필요없는 경우도 있음 -> 자동화 count쿼리는 원본쿼리와 함꼐 조인 -> 성능 저하 ->
   -> 필요하면 count 전용 쿼리 별도 작성
7. on절 사용(조인 대상 필터링) -> where조인 필터링하는것과 통일 -> on절을 사용할떄
   -> 내부조인이면 where절 사용 -> 꼭 필요한 경우에만 외부조인하여 on절 사용